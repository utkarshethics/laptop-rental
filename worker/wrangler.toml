name = "laptoprent-webhook"
main = "src/index.ts"
compatibility_date = "2025-01-01"

[vars]
CRM_ON_FAILED = "0"
# When a dedicated LaptopRent payment link is created (user action), set its
# plink id here so the webhook accepts payments against it:
# LAPTOP_RENT_LINK_IDS = "plink_XXX"