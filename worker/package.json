{
  "name": "laptoprent-razorpay-webhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788 --ip 127.0.0.1",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250924.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.90.0"
  }
}